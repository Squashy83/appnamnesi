type Doctor {
  id: String
  name: String!
  surname: String!
}

input CreateDoctorDTO {
  name: String!
  surname: String!
}

type Query {
  doctors: [Doctor!]
}

type Mutation {
  createDoctor(input: CreateDoctorDTO!): Doctor
}

type Mutation {
  deleteDoctor(_id: String!): Boolean
}
